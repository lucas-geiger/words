---
description: Build the site for production
---

Build the static site for production deployment.

Execute: `bun astro build`

This will:

- Type check all content
- Build static pages
- Optimize assets
- Output to `dist/` directory

After building, you can preview with `bun astro preview` to test the production build locally.
